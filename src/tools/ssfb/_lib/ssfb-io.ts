// Shared pieces for the SSFB-only tools. The connector context helpers moved
// to src/tools/_lib/connector-context.ts when the field-crypto tools stopped
// being SSFB-only (D48).

/** The only entity cbs_call and the statement tools serve. */
export const SSFB = 'ssfb';
