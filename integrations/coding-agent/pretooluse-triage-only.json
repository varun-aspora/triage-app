{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node -e 'let s=\"\";process.stdin.on(\"data\",(d)=>{s+=d}).on(\"end\",()=>{let c=\"\";try{c=String(JSON.parse(s).tool_input.command??\"\")}catch{}if(/^triage [^;&|`$<>\\\\\\r\\n]*$/.test(c))process.exit(0);process.stderr.write(\"Only triage commands may run in this workspace. Shell operators, redirects and $ are refused.\\n\");process.exit(2)})'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
